---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/config/database.ts
  - server/src/models/movie.ts
  - server/src/config/schema.sql
  - server/src/types/movie.ts
  - shared/types/index.ts
autonomous: true
must_haves:
  truths:
    - SQLite database exists with movies table
    - Schema supports extensible metadata via JSON column
    - TypeScript types are defined for all database entities
    - Database connection is configured and tested
  artifacts:
    - path: server/src/config/database.ts
      provides: Database connection and query utilities
      exports: ["getDb", "initDatabase"]
    - path: server/src/config/schema.sql
      provides: Complete database schema definition
      contains: ["CREATE TABLE movies", "CREATE INDEX"]
    - path: server/src/types/movie.ts
      provides: TypeScript interfaces for Movie and related entities
      exports: ["Movie", "MovieMetadata", "CreateMovieInput"]
    - path: shared/types/index.ts
      provides: Shared types between client and server
      exports: ["Movie", "MovieResponse"]
  key_links:
    - from: server/src/config/database.ts
      to: server/src/config/schema.sql
      via: "executes schema on init"
      pattern: "CREATE TABLE IF NOT EXISTS"
---

<objective>
Create SQLite database schema with extensible metadata support and TypeScript types for the movie library system.

Purpose: Establish the data foundation that all other features depend on. The schema must support Phase 2 metadata enrichment while being flexible for future extensions.
Output: Database configuration, schema definition, and TypeScript types.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md

**Phase Context:**
Phase 1: Foundation — Infrastructure exists for secure file storage, database operations, and access control with admin upload capability.

**Key Requirements:**
- META-07: Metadata is stored in extensible format allowing future field additions
- Files will be stored securely with UUIDs
- Extensible JSON metadata for director, actors, genres, etc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database configuration</name>
  <files>server/src/config/database.ts</files>
  <action>Create database configuration module that:
1. Initializes SQLite connection with proper settings (WAL mode for concurrency)
2. Provides getDb() function to get database instance
3. Handles connection errors gracefully
4. Creates database file in project root (data/movies.db)
5. Exports typed query helper functions

Use sqlite3 package with Promise wrapper for async/await support. Set pragma for foreign keys and WAL mode.</action>
  <verify>Run initDatabase() and verify database file is created in data/movies.db</verify>
  <done>Database module exists with getDb() function, connection tested successfully</done>
</task>

<task type="auto">
  <name>Task 2: Define database schema</name>
  <files>server/src/config/schema.sql</files>
  <action>Create complete database schema:

1. movies table with:
   - id (INTEGER PRIMARY KEY AUTOINCREMENT)
   - file_id (TEXT UNIQUE NOT NULL) - UUID for secure file access
   - file_path (TEXT NOT NULL) - actual filesystem path
   - file_size (BIGINT) - size in bytes
   - file_type (TEXT) - video format
   - original_title (TEXT)
   - italian_title (TEXT)
   - release_year (INTEGER)
   - runtime (INTEGER) - minutes
   - plot_summary (TEXT)
   - poster_path (TEXT) - path to cached poster
   - metadata_json (TEXT) - JSON for extensible fields (director, actors, genres, etc.)
   - created_at (DATETIME DEFAULT CURRENT_TIMESTAMP)
   - updated_at (DATETIME DEFAULT CURRENT_TIMESTAMP)

2. Indexes for performance:
   - idx_movies_file_id (for secure lookups)
   - idx_movies_original_title (for search)
   - idx_movies_release_year (for filtering)
   - idx_movies_metadata (for JSON queries)

3. Migration tracking table:
   - migrations table to track schema versions</action>
  <verify>Execute schema.sql against database and verify all tables created with: SELECT name FROM sqlite_master WHERE type='table';</verify>
  <done>Schema file exists, all tables and indexes created successfully in database</done>
</task>

<task type="auto">
  <name>Task 3: Create TypeScript type definitions</name>
  <files>server/src/types/movie.ts, shared/types/index.ts</files>
  <action>Create comprehensive TypeScript interfaces:

1. Movie interface (database model):
   - All fields matching schema
   - Proper types (string, number, Date, etc.)
   - Nullable fields marked with ?

2. MovieMetadata interface (extensible):
   - director?: string
   - actors?: string[]
   - genres?: string[]
   - rating?: number
   - originalLanguage?: string
   - productionCountries?: string[]
   - Allow additional properties via index signature

3. CreateMovieInput interface:
   - Fields required for creating a movie
   - Omit auto-generated fields (id, timestamps)

4. UpdateMovieInput interface:
   - Partial type for updates
   - All fields optional

5. Shared types in shared/types/index.ts for client usage

Ensure types are strict and match database schema exactly.</action>
  <verify>TypeScript compiles without errors: npx tsc --noEmit in server directory</verify>
  <done>All type interfaces defined, TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 4: Create database initialization script</name>
  <files>server/src/scripts/init-db.ts</files>
  <action>Create initialization script that:
1. Reads schema.sql file
2. Executes against database
3. Handles errors gracefully
4. Logs success/failure
5. Can be run via npm script

This will be used to initialize fresh databases and for testing.</action>
  <verify>Run script and verify: database created, schema applied, success logged</verify>
  <done>Init script works, creates database with correct schema</done>
</task>

<task type="auto">
  <name>Task 5: Create database query helpers</name>
  <files>server/src/repositories/movieRepository.ts</files>
  <action>Create repository pattern for movie operations:

1. findById(id: number): Promise<Movie | null>
2. findByFileId(fileId: string): Promise<Movie | null>
3. findAll(): Promise<Movie[]>
4. create(input: CreateMovieInput): Promise<Movie>
5. update(id: number, input: UpdateMovieInput): Promise<Movie>
6. delete(id: number): Promise<void>
7. search(query: string): Promise<Movie[]> - basic title search

Use parameterized queries to prevent SQL injection. Return properly typed Movie objects. Handle JSON serialization for metadata field.</action>
  <verify>Write unit tests or manual test script that exercises each method, verify CRUD operations work</verify>
  <done>Repository methods implemented, basic CRUD tested</done>
</task>

</tasks>

<verification>
**Overall phase checks:**
1. Database file created and accessible
2. All schema tables exist (movies, migrations)
3. Indexes created for performance
4. TypeScript types compile without errors
5. Repository methods tested and working

**Test commands:**
```bash
# Initialize database
npm run db:init

# Verify schema
sqlite3 data/movies.db ".schema"

# Type check
cd server && npx tsc --noEmit
```
</verification>

<success_criteria>
- Database configuration module exports getDb() function
- Schema.sql defines movies table with all required fields
- JSON metadata column supports extensibility
- TypeScript interfaces match database schema
- Repository provides basic CRUD operations
- All code passes TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
